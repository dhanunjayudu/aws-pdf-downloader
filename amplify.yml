version: 1
frontend:
  phases:
    build:
      commands:
        - echo "FastAPI UI deployment - static HTML with FastAPI backend integration"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - '**/*.css'
      - '**/*.js'
      - '**/*.png'
      - '**/*.jpg'
      - '**/*.gif'
